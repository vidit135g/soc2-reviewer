# Browser-visible — used by client fetches to the Next.js rewrite.
NEXT_PUBLIC_API_BASE=/api/backend

# Server-only — used by Next.js API rewrites to reach the backend.
BACKEND_URL=http://backend:8000
